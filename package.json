{
  "name": "nodemelivre-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "NodeMeLivre: SDK TypeScript modular para a API do Mercado Livre",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "build": "npm run build --workspace @nodemelivre/errors && npm run build --workspace @nodemelivre/core && npm run build --workspace @nodemelivre/http && npm run build --workspace @nodemelivre/types && npm run build --workspace @nodemelivre/auth && npm run build --workspaces --if-present --workspace @nodemelivre/items --workspace @nodemelivre/orders --workspace @nodemelivre/users --workspace @nodemelivre/shipments --workspace @nodemelivre/questions --workspace @nodemelivre/images && npm run build --workspace @nodemelivre/sdk",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
